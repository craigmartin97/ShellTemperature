namespace BluetoothService.Enums
{
    /// <summary>
    /// Enum to indicate the state the bluetooth connection is in
    /// </summary>
    public enum DeviceConnectionStatus
    {
        FAILED, CONNECTED, CONNECTING, PAUSED
    }
}